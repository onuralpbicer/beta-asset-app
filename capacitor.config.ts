import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.beta.bakim.app',
    appName: 'beta-asset-app',
    webDir: 'www',
    server: {
        androidScheme: 'https',
    },
}

export default config
